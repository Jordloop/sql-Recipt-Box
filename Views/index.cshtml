<h1>Recipes.</h1>
<ul>
  @if (@Model.Count != 0)
  {
    @foreach(var recipe in Model)
    {
      <li>@recipe.GetName()</li>
    }
  }
  else
  {
      <h3>You have no recipes, order pizza.</h3>
  }
</ul>

<a href="/add/recipe">Add Recipe</a>
